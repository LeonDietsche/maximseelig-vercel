# Maxim Seelig website

## Development

```bash
npm install
npm run dev -- --host
```

## Production build

```bash
npm run build
```

The website source is in `index.html` and `public/`. The `node_modules` and `dist` folders are intentionally excluded because they can be regenerated.
